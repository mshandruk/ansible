all:
  children:
    dockers:
      hosts:
        debian.test.lab:
        fedora.test.lab:
        centos7.test.lab:
        centos9.test.lab:
        ubuntu.test.lab:
    gateway:
      hosts:
        gw.test.lab:
    kvm:
      hosts:
        debian.test.lab:
        ubuntu.test.lab:
